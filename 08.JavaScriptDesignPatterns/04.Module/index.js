/* Module Pattern: This pattern helps organize code by breaking it into smaller, manageable modules. 
It's like dividing a big book into chapters for easier reading and understanding. */

import { add, multiply, substract, square } from "./math.js";

add(2, 3);
substract(3, 2);
square(5);
multiply(2, 5);
